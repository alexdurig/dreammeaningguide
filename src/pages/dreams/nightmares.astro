---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const dreams = (await getCollection("dreams"))
  .filter(d => d.data.category === "nightmares");
---

<BaseLayout title="Nightmare Dreams">

  <section style="
    background: #333;
    color: white;
    padding: 3rem 1rem;
    text-align: center;
    border-radius: 0 0 12px 12px;
  ">
    <h1 style="font-size: 2.2rem; margin-bottom: 0.5rem;">
      😱 Nightmares
    </h1>
    <p style="font-size: 1.1rem; opacity: 0.9;">
      Nightmares reveal fear, avoidance, danger, and the shadow side of the psyche.
    </p>
  </section>

  <section style="max-width: 900px; margin: 3rem auto;">
    <ul style="list-style: none; padding: 0;">
      {dreams.map(d => (
        <li style="margin-bottom: 0.8rem;">
          <a href={`/dreams/${d.slug}`} style="
            text-decoration: none;
            color: #333;
            font-size: 1.1rem;
            padding: 0.6rem 0.4rem;
            display: block;
            border-bottom: 1px solid #eee;
          ">
            {d.data.title}
          </a>
        </li>
      ))}
    </ul>
  </section>

</BaseLayout>

